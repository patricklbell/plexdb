# Skiplist — upstream conformance tests deselected because plexdb's parser
# is intentionally more permissive than Cassandra's for these shapes.
#
# Format: one pytest nodeid substring per line (matched with `in item.nodeid`),
# optionally followed by `# reason`. Blank lines and `#` comments are ignored.
#
# Only list tests that exercise a parsing gap that is acceptable to leave open
# (i.e. the parser does not reject something Cassandra rejects with
# SyntaxException, and writing bespoke logic for it is not justified). Do NOT
# list tests that exercise genuinely fatal parsing (unparsable / ambiguous
# input) — those must continue to run.
#
# Regenerate or extend by hand. Loaded by tools/cql_tests/conftest.py.

alter_test.py::testDoubleWith    # permissive parser: accepts `ALTER KEYSPACE WITH WITH ...`
create_test.py::testDoubleWith   # permissive parser: accepts `CREATE KEYSPACE WITH WITH ...`
