[pytest]
# Minimal pytest config for running redis-py's tests against resp_server.
# Many tests will fail for unimplemented commands; that is expected and
# informative — failures show what still needs to be implemented.
addopts = --no-header
