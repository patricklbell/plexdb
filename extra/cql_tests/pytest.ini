[pytest]
# Minimal pytest config for running ScyllaDB's CQL tests against objstore.
# Tests are expected to mostly fail/xfail since objstore implements a subset
# of CQL. Progress is tracked by counting passes over time.
addopts = --no-header
