Install `grpc-devel` and `protobuf-devel`. You may need to install `zlib-static` if `libz.a` is missing.

See https://github.com/open-telemetry/opentelemetry-cpp/blob/main/INSTALL.md for more details.